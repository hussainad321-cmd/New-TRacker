# Copy to .env and set real values before running in production
DATABASE_URL=postgres://user:password@hostname:5432/garmentflow
JWT_SECRET=replace_with_a_strong_random_value
PORT=5000
NODE_ENV=production

## Packages
recharts | Dashboard analytics and process flow visualization
date-fns | Formatting dates in tables
framer-motion | Page transitions and UI animations
clsx | Utility for conditional classes (used with tailwind-merge)
tailwind-merge | Merging tailwind classes intelligently

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
